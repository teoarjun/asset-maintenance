# Docker (docker compose up -d): DATABASE_URL="postgresql://asset:asset@localhost:5432/asset_maintenance"
# Postgres.app (local): create DB first, then e.g. postgresql://YOUR_MAC_USERNAME@localhost:5432/asset_maintenance
DATABASE_URL="postgresql://asset:asset@localhost:5432/asset_maintenance"
PORT=4000
JWT_SECRET=change-me-in-production-use-long-random-string
PUBLIC_API_KEY=demo-public-api-key-change-in-production
